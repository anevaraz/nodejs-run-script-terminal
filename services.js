module.exports = {
  exampleFunction: () => {
    console.log('this function is running script through of terminal command line')
  },
  exampleFunctionWithParam: (param) => {
    console.log(`this function is running script through of terminal command line with param '${param}'`)
  }
}